# Build-only dependencies (PyInstaller packaging of the Windows EXE).
# Not needed to RUN the app -- install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-build.txt
# pefile / pywin32-ctypes are Windows-only and will not resolve on Linux;
# they are only used by the PyInstaller Windows build.
-r requirements.txt

pyinstaller==6.20.0
pyinstaller-hooks-contrib==2026.5
altgraph==0.17.5
setuptools==82.0.1

# Windows-only build helpers.
pefile==2024.8.26; sys_platform == "win32"
pywin32-ctypes==0.2.3; sys_platform == "win32"
